'use strict';

class BinaryTree{
    constructor(root = null) {
        this.root = root;
    }
    // root >> left >> right 
    preOrder() {
        let arr = [];
        const traversal = node => {
            arr.push(node.value);
            if()
        }
    }
    postOrder() {
        
    }
    inOrder() {
        
    }
}

